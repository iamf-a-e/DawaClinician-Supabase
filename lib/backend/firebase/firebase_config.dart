import '/backend/supabase/supabase_config.dart';

/// Legacy FlutterFlow entry point kept for generated imports.
/// The app now initializes Supabase instead of Firebase.
Future initFirebase() => initSupabase();
